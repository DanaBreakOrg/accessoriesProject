package myPackage;

import io.cucumber.java.en.Given;
import io.cucumber.java.en.When;
import io.cucumber.java.en.Then;
import static org.junit.Assert.*;
import org.example.Customer;

public class customerShopping {

    private Customer customer;
/*
    @Given("a customer is logged in")
    public void a_customer_is_logged_in() {
        // Initialize a Customer instance
        customer = new Customer("username", "password", "address", "phone", "email", "gender", 0.0, 1);
    }

    @When("the customer chooses to view their installation request status")
    public void the_customer_chooses_to_view_installation_request_status() {
        // Code to view installation request status
        // Example: customer.viewInstallationRequestStatus();
    }

    @Then("the status of all installation requests made by the customer is displayed")
    public void installation_requests_status_is_displayed() {
        // Assert that the installation request status is displayed
        // Example: assertNotNull(customer.getInstallationRequestStatus());
    }

    @When("the customer submits an installation request for product with ID {string}")
    public void the_customer_submits_installation_request(String productId) {
        // Code to submit installation request
        // Example: customer.submitInstallationRequest(productId);
    }

    @Then("an installation request for product {string} should be recorded")
    public void installation_request_should_be_recorded(String productId) {
        // Assert that the installation request for the product is recorded
        // Example: assertTrue(customer.isInstallationRequestRecorded(productId));
    }

    @When("the customer chooses to logout")
    public void the_customer_chooses_to_logout() {
        // Code to handle customer logout
        // Example: customer.logout();
    }

    @Then("the customer should be logged out of the system")
    public void customer_is_logged_out() {
        // Assert that the customer is logged out
        // Example: assertFalse(customer.isLoggedIn());
    }*/
}
